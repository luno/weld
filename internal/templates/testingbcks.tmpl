package {{.Package}}

// Code generated by weld. DO NOT EDIT.

import (
    "testing"

	"github.com/luno/jettison/errors"
{{range .Imports -}}
	{{if .Aliased}}{{.Name}} {{end}}"{{.PkgPath}}"
{{end}}
)

type Backends interface {
{{range .Deps -}}
  {{.Getter}}() {{.Type}}
{{end}}

// Setters that allow setting the implementation of the above getter for a single test case. On t.Cleanup
// the dependency is reset to it's original value / implementation.
{{range .Deps -}}
  Set{{.Getter}}ForTesting(t *testing.T,  {{.Var}} {{.Type}})
{{end}}
}
